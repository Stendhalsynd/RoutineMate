{
  "name": "@routinemate/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "echo 'domain lint placeholder'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --import tsx test/*.test.ts",
    "build": "echo 'domain build placeholder'"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
