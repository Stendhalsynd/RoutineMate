# S0-3 API Contract Baseline

## Given
- The backend receives payloads for quick logs and goal updates.

## When
- Contract schemas validate incoming endpoint inputs.

## Then
- Valid payloads parse to strongly typed DTOs.
- Invalid payloads fail with schema-level errors.
- Date-range queries enforce from-to ordering.
