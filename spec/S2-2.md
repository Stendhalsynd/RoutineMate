# S2-2 최근/어제 복사 UX

## Given
- 사용자에게 유효한 세션이 있고 과거 식단 로그가 존재한다.

## When
- "어제 기록 복사" 또는 "최근 기록 복사" 버튼을 누른다.

## Then
- 가장 최근/해당 날짜의 식단 로그가 폼으로 채워진다.
- 대상 로그가 없으면 안내 메시지가 표시된다.
- 복사 결과 후 저장 버튼으로 동일한 로그를 바로 제출할 수 있다.

