# S3-3 대시보드 7/30/90 집계 정합성 강화

## Given
- 사용자는 식단/운동/체성분 로그를 여러 날짜에 기록했다.
- 대시보드는 7일/30일/90일 범위를 선택할 수 있다.

## When
- 사용자가 기간 토글(7d/30d/90d)을 변경한다.

## Then
- 선택한 기간의 시작일/종료일 기준으로 집계가 정확히 계산된다.
- `daily` 길이는 선택 기간 일수와 정확히 일치한다.
- 집계 메타(기간, 시작/종료일, 포함 로그 수, 로그 존재 일수)가 함께 제공된다.
