# S1-2 목표 설정 UI 고도화

## Given
- 사용자에게 유효한 세션이 있다.
- 목표 조회 API가 기존 목표(주간 루틴, D-day, 체중, 체지방)를 반환한다.

## When
- 목표 설정 폼에서 각 항목(주간 루틴/체중/체지방/D-day)을 수정하고 저장 버튼을 누른다.

## Then
- `POST /v1/goals`가 201을 반환한다.
- 현재 목표 패널과 대시보드 요약이 업데이트된다.
- 비정상 입력(범위 초과, 빈 주간 루틴 목표)이면 실패 메시지가 노출된다.

