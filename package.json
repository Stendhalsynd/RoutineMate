{
  "name": "routinemate",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present && npm run test:unit",
    "test:unit": "node --test --import tsx tests/unit/*.test.ts",
    "firebase:emulators": "firebase emulators:start --only auth,firestore",
    "firestore:migrate:notion": "tsx scripts/migrate-notion-to-firestore.ts",
    "firestore:migrate:notion:dry-run": "tsx scripts/migrate-notion-to-firestore.ts --dry-run",
    "build": "npm run build --workspaces --if-present",
    "build:apk:preview": "bash scripts/build-apk.sh preview",
    "build:apk:release": "bash scripts/build-apk.sh release",
    "release:apk:github": "bash scripts/release-apk.sh",
    "notion:schema:check": "node scripts/check-notion-schema.mjs",
    "release:notes": "node scripts/generate-release-notes.mjs",
    "verify:deploy:prod": "npx playwright test --config=playwright.deploy.config.ts",
    "deploy:prod:verify": "bash scripts/deploy-and-verify.sh"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "@playwright/test": "^1.58.2",
    "expo": "~52.0.0",
    "firebase": "^12.11.0",
    "firebase-admin": "^13.4.0",
    "firebase-tools": "^14.8.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
